export type PropsButton = {
    textButton: string;
}

export type PropsImagesBlock = {
	mainimage: string;
	mainimagealt: string;
	video: string;
	videoalt: string;
	light: string;
	bg?: string;
}

export type PropsFaqItem = {
  title: string;
  text: string;
};